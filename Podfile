# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pokemon_with_widget' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for pokemon_with_widget
  pod 'PokemonAPI'
  pod 'netfox'

  target 'pokemon_with_widgetTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pokemon_with_widgetUITests' do
    # Pods for testing
  end

end
